{
  "name": "drive-in-car-seed",
  "version": "0.1.0",
  "private": true,
  "description": "Drive in Car — Firestore seed scripts (courses, meta)",
  "scripts": {
    "seed": "ts-node seed.ts",
    "seed:meta": "ts-node seed.ts --only=meta",
    "seed:courses": "ts-node seed.ts --only=courses"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
